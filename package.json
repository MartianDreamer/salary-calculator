{
  "scripts": {
    "watch": "tsup src/index.ts --format iife --target es6 --watch --no-splitting --global-name App",
    "build": "tsup src/index.ts --format iife --target es6 --no-splitting --global-name App --minify"
  },
  "devDependencies": {
    "@swc/core": "^1.15.43",
    "tsup": "^8.5.1",
    "typescript": "^6.0.3"
  }
}